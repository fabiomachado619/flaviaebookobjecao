/** Exit intent popup — desligado temporariamente. */
export const ENABLE_EXIT_POPUP = false;

/** Botão flutuante do WhatsApp — desligado temporariamente. */
export const ENABLE_WHATSAPP_FLOAT = false;
